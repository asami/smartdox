# docs/notes

Purpose: exploratory, non-normative design and implementation proposals.

Notes may inform a later design or specification, but do not themselves define
SmartDox behavior. Current execution state belongs in `docs/phase`; historical
trace belongs in `docs/journal`.
